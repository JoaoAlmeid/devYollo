{
    "PORT": {
      "required": true,
      "description": "Define a porta em que o servidor está rodando",
      "default": 3007,
      "validators": [
        {
          "name": "numeric"
        }
      ]
    },
    "NODE_ENV": {
      "required": true,
      "description": "This defines the current environment, one of: develop, homolog, beta",
      "validators": [
        {
          "name": "in",
          "options": ["develop", "staging", "production"]
        }
      ]
    },
    "DB_DEBUG": {
      "required": true,
      "default": false,
      "description": "Debug flag to show aditional logs"
    },
    "DB_DIALECT": {
      "required": true,
      "description": "Database driver"
    },
    "DB_HOST": {
      "required": true,
      "description": "Database host"
    },
    "DB_PORT": {
      "required": true,
      "description": "Database port"
    },
    "DB_USER": {
      "required": true,
      "description": "Database username"
    },
    "DB_PASS": {
      "required": true,
      "description": "Database password"
    },
    "DB_NAME": {
      "required": true,
      "description": "Database schema"
    }
  }